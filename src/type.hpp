#ifndef TYPE_H
#define TYPE_H

#include "debug.hpp"

#define MAX_LVL 7

typedef float cf_T;	// floating point coordinate
typedef int cd_T;	// discrete coordinate

typedef unsigned int sid_T;	// shape id
typedef unsigned int oid_T;	// offset id

#endif
